multiline_comment|/* Should not be needed. IDE stupidity */
multiline_comment|/* JMA 18.05.03 - is kinda needed, if only to tell it we don&squot;t have a PCI bus */
DECL|macro|PCI_DMA_BUS_IS_PHYS
mdefine_line|#define PCI_DMA_BUS_IS_PHYS  0
eof
